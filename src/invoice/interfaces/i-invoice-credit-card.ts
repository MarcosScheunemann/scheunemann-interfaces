export interface IInvoiceCreditCard {
  // #region Properties (6)

  bin: string;
  brand: string;
  capturedAt: string;
  last4: string;
  tid: string;
  transaction: string;

  // #endregion Properties (6)
}
