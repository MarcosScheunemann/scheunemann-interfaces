export interface IIuguPlanPrice {
    // #region Properties (5)

    createdAt: Date;
    currency: string;
    id: string;
    updatedAt: Date;
    valueCents: number;

    // #endregion Properties (5)
}
