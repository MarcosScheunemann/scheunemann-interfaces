export interface IIuguSubscriptionRecentInvoice {
    // #region Properties (5)

    dueDate: Date;
    id: string;
    secureUrl: string;
    status: string;
    total: string;

    // #endregion Properties (5)
}
