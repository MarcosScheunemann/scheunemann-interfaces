export interface IIuguChangePlanSimulation {
    // #region Properties (6)

    cost: number;
    cycles: number;
    discount: number;
    expiresAt: string;
    newPlan: string;
    oldPlan: string;

    // #endregion Properties (6)
}
