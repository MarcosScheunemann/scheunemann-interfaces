import { IIuguMarketPlaceTokens } from './i-marketplace-tokens';

export interface IIuguMarketPlaceToken {
    // #region Properties (7)

    [key: string]: IIuguMarketPlaceTokens;

    // #endregion Properties (7)
}
