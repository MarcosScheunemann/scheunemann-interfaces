export interface IDeliveryAreaFixed {
    // #region Properties (5)

    active: boolean;
    deliveryFee: number;
    maxTime: number;
    minTime: number;
    minimumOrderValue: number;

    // #endregion Properties (5)
}
