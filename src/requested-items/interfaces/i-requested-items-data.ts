export interface IRequestedItemData {
  // #region Properties (5)

  description: string;
  quantity: number;
  totalPrice: number;
  unit: string | null;
  unitPrice: number;

  // #endregion Properties (5)
}
