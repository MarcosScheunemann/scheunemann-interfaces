export interface IBlueSoftCommercialUnit {
  // #region Properties (10)
  typePackaging: string;
  quantityPackaging: number;
  ballast: string | null;
  layer: string | null;

  // #endregion Properties (10)
}
